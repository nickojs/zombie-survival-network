const CONTAINER_WIDTH = 400;
const CONTAINER_HEIGHT = 500;

export const right = window.innerWidth - CONTAINER_WIDTH;
export const below = window.innerHeight - CONTAINER_HEIGHT - 45;
export const left = 0;
